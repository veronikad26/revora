"""
Streamlit dashboard (PRD Section 10 — Tech Stack).

Responsibilities:
- Display the three-condition comparison (do-nothing vs. naive vs.
  Revora): rupees recovered, uplift multiple.
- Breakdown views by root-cause category and by entry point.
- PTP kept-rate, false-intervention count, escalation breakdown.
- Blocked-message log (Trust Firewall pass/fail history).
- Closed-loop confidence/timing table, before vs. after.
- "One fully-traced case" view: walks a single case_id through the
  full graph (audit trail) for the live demo.

No implementation yet — skeleton only.
"""
