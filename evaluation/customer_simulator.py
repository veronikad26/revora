"""
Customer response simulator (PRD Section 9.2).

Responsibilities:
- A weighted-probability function that generates a simulated customer
  reaction (accept a retry, reply to a nudge, honor a PTP) according
  to a case's hidden recoverability profile.
- No second LLM persona — logic must be simple enough to show
  directly to judges as a transparency artifact.

No implementation yet — skeleton only.
"""
